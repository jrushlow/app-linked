<form method="post" action="{{ path('app_me_delete', {'id': me.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ me.id) }}">
    <button class="btn">Delete</button>
</form>
